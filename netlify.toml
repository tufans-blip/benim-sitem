# Netlify root config: run build inside `_deploy` and publish the generated `dist`
[build]
  command = "cd _deploy && npm install sharp && npm run build"
  publish = "_deploy/dist"

[build.environment]
  NODE_VERSION = "20"
# Force rebuild 15:25:44
